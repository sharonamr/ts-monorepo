{
  "name": "ts-monorepo",
  "private": true,
  "scripts": {
    "postinstall": "npx lerna bootstrap",
    "lint": "npx lerna run lint --stream",
    "lint:fix": "npx lerna run lint:fix --stream",
    "test": "npx lerna run test --scope=app --stream",
    "start": "npx lerna run start --scope=app --stream",
    "build": "npx lerna run build --scope=app --stream"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@typescript-eslint/eslint-plugin": "^4.28.5",
    "@typescript-eslint/parser": "^4.28.5",
    "eslint": "^7.31.0",
    "jest": "^27.0.6",
    "lerna": "^4.0.0",
    "ts-jest": "^27.0.4",
    "typescript": "^4.3.5"
  }
}
